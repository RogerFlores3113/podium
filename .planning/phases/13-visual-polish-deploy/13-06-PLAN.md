---
phase: 13-visual-polish-deploy
plan: 06
type: execute
wave: 4
depends_on:
  - 13-05
files_modified:
  - README.md
autonomous: false
requirements:
  - POLISH-04
  - POLISH-05
must_haves:
  truths:
    - "README.md has an Architecture section with tech stack rationale legible to a recruiter"
    - "The Architecture section explains WHY each tech choice was made, not just what it is"
    - "Clerk is switched to Production mode (env vars updated in Vercel dashboard)"
    - "Live URL verified working end-to-end in guest mode after Clerk switch"
  artifacts:
    - path: "README.md"
      provides: "Architecture section with tech stack rationale"
      contains: "## Why this stack"
  key_links:
    - from: "README.md"
      to: "Architecture section"
      via: "## Why this stack heading"
      pattern: "Why this stack"
---

<objective>
Add a recruiter-legible architecture rationale section to README.md (POLISH-04) and guide the human through switching Clerk to Production mode and verifying the live URL end-to-end (D-10 / POLISH-05).

Purpose: A recruiter reading the repo should understand not just WHAT was built but WHY each technical decision was made. The Clerk Production switch is a manual prerequisite for the app to be publicly shareable without Clerk's Development mode warnings.
Output: Updated README.md with "Why this stack" section; Clerk Production mode active; live URL verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-polish-deploy/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Why this stack" architecture rationale to README.md (POLISH-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — existing sections, architecture diagram, stack table)
    - .planning/phases/13-visual-polish-deploy/13-CONTEXT.md (specifics section — "production-ready definition")
  </read_first>
  <action>
The README already has an architecture diagram and a stack table. Add a new "## Why this stack" section AFTER the existing `## Architecture` section and BEFORE `## What I built and learned`. This section should be ~250-350 words and explain the rationale for each key technology choice in terms a recruiter can understand — not just listing tools, but explaining tradeoffs and decisions.

Insert the following section:

```markdown
## Why this stack

**FastAPI + SSE streaming** — FastAPI's async-first design pairs naturally with server-sent events. When the agent chains multiple tool calls before responding, the user sees real-time progress rather than a blank wait. A synchronous framework would have required polling or WebSockets with more complexity.

**LiteLLM** — Single interface for OpenAI, Anthropic, and local Ollama models. Adding a new provider is one line of config, not a new integration. The alternative (direct SDK calls per provider) would have meant 3x the API-interaction code to maintain.

**pgvector with HNSW index** — Postgres already handles auth, conversations, and documents — adding pgvector avoids a separate vector database service, reducing infra cost and operational complexity. HNSW (hierarchical navigable small world) gives O(log n) approximate nearest-neighbor search without a full index scan.

**arq (async Redis queue) + Valkey** — Memory extraction runs in the background after each conversation without blocking the SSE stream. arq is a lightweight job queue that runs in the same Python process ecosystem; Valkey is a Redis-compatible open-source fork that replaced ElastiCache at ~10% of the cost on a t4g.nano.

**Clerk** — Auth is the highest-risk surface to hand-roll. Clerk provides JWKS-backed JWT verification, session management, and social login. The tradeoff is a vendor dependency; the mitigation is that the custom HS256 guest JWT path shows the underlying auth mechanics clearly.

**Next.js 14 App Router + Tailwind** — App Router supports React Server Components for static routes (landing, settings) while client components handle streaming chat. Tailwind's utility classes keep the component CSS co-located and avoid stylesheet sprawl.

**E2B sandboxed Python execution** — The alternative (subprocess on the API server) is a security hole. E2B runs user code in an isolated VM with a timeout; the only attack surface is what the agent constructs as a code string, which is already constrained by the system prompt.

**AWS ECS Fargate on public subnets** — Avoids NAT Gateway charges ($32+/mo) by assigning a public IP to each task. The tradeoff is that security groups must be strict (they are: only ports 80/443 from the ALB). This cut infrastructure cost from ~$120/mo to ~$45/mo.
```
  </action>
  <verify>
    <automated>grep -n "Why this stack" /home/rflor/podium/README.md</automated>
  </verify>
  <done>README.md contains a "## Why this stack" section with at least 6 technology rationale paragraphs. Each paragraph names the technology, explains the decision, and notes the tradeoff considered.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Switch Clerk to Production mode (D-10)</name>
  <action>
This task requires manual action in the Clerk dashboard. Claude cannot access external dashboards.

**Prerequisites:** All code changes from Plans 13-01 through 13-05 must be merged and deployed to Vercel before doing this step. Changing Clerk keys with old code deployed may break authentication.

**Steps:**

1. **Create a Clerk Production instance:**
   - Go to [dashboard.clerk.com](https://dashboard.clerk.com)
   - Select your Podium application
   - Click "Switch to Production" or create a new Production instance
   - Configure the same social providers (if any) and settings as Development

2. **Get the Production keys:**
   - In the Clerk dashboard → API Keys
   - Copy `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` (starts with `pk_live_`)
   - Copy `CLERK_SECRET_KEY` (starts with `sk_live_`)

3. **Update Vercel environment variables:**
   - Go to [vercel.com/dashboard](https://vercel.com/dashboard) → podium-beta project
   - Settings → Environment Variables
   - Update `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` with the `pk_live_` value
   - Update `CLERK_SECRET_KEY` with the `sk_live_` value

4. **Update the backend environment (AWS ECS):**
   - The backend also uses `CLERK_SECRET_KEY` for JWT verification
   - Update the value in AWS Secrets Manager or ECS task definition environment variables
   - Trigger a new ECS deployment to pick up the new secret

5. **Trigger a Vercel redeployment:**
   - Either push a commit or manually trigger a redeploy in Vercel dashboard
   - Wait for deployment to complete

6. **Verify Clerk Production mode is active:**
   - Visit `https://podium-beta.vercel.app`
   - Open browser DevTools → Network
   - Sign in or create an account
   - Confirm no "development mode" warnings appear in the Clerk widget
  </action>
  <resume-signal>Type "clerk production active" when Clerk Production mode is configured and the redeployment is complete</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end guest mode verification (POLISH-05)</name>
  <what-built>
All Phase 13 code changes are merged and deployed. Clerk is in Production mode. This checkpoint verifies the live URL works end-to-end in guest mode — the full user journey a recruiter would experience.
  </what-built>
  <how-to-verify>
**Test as a fresh guest user (incognito window):**

1. Open `https://podium-beta.vercel.app` in an incognito/private browsing window
2. Click "Try as guest" on the landing page
3. Verify: you land in a chat session with the new Podium-specific welcome message (should mention "search the web", "memory", "code", "documents" — NOT the old generic "personal AI assistant" copy)
4. Verify: capability cards show (should NOT include "I'll remember this" / "Remember something" for guest mode)
5. Send a message: "Search the web for the current price of Bitcoin"
6. Verify: a "Searching the web…" status label appears
7. Verify: a response with the Bitcoin price (or close equivalent) renders in markdown
8. Test mobile: resize to < 768px or use DevTools mobile emulation
9. Verify: sidebar is collapsed, hamburger ☰ button is visible
10. Tap ☰: verify sidebar slides in as overlay
11. Tap backdrop: verify sidebar closes
12. Double-click a conversation title (if visible): verify inline rename input appears
13. Type a new name and press Enter: verify the title updates
14. Verify: no Clerk "Development mode" warnings appear in the UI

**If any step fails:** note which step and what happened. The phase is not complete until all steps pass.
  </how-to-verify>
  <resume-signal>Type "e2e verified" if all steps pass, or describe which steps failed and what was observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel env vars → Clerk keys | Production keys must only be in env vars, never in code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Information Disclosure | Clerk key rotation | mitigate | Production keys must be set as Vercel environment variables only; never committed to git; `.env` is gitignored |
| T-13-13 | Tampering | README.md architecture section | accept | Documentation; no security surface |
</threat_model>

<verification>
- `grep -n "Why this stack" /home/rflor/podium/README.md` — returns match
- `grep -c "FastAPI\|LiteLLM\|pgvector\|arq\|Clerk\|Next.js\|E2B\|ECS" /home/rflor/podium/README.md` — returns 8+ (at least one per technology)
- Human checkpoint: Clerk Production active
- Human checkpoint: E2E guest mode verification passed
</verification>

<success_criteria>
- POLISH-04: README.md has "## Why this stack" section with rationale for at least 6 technology choices
- D-10: Clerk Production keys (`pk_live_` / `sk_live_`) are active in Vercel and ECS
- POLISH-05: Live URL verified end-to-end in guest mode — all 14 verification steps pass
- Backend: 94+ tests passing; Frontend: 67+ tests passing
- No TypeScript errors across the frontend codebase
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-polish-deploy/13-06-SUMMARY.md`
</output>
